[toolchain]

[features]
resolution = true
skip-lint = false

[programs.localnet]
tax_token = "C4ZgZJSwHg65gZsLoa9gt7nitzeMFRMD6eK6xMEgdyPg"

[registry]
url = "https://api.apr.dev"

[provider]
# cluster = "localnet"
# cluster = "devnet"
cluster = "mainnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
